<?xml version="1.0" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rodolfo.apptec" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/logoTheme" >
        <activity
            android:name=".LogIn"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".EmployeeRegistration" />
        <activity android:name=".Laucher" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RelatorioFot_activity" />
        <activity
            android:name=".SearchForReport"
            android:label="@string/title_activity_search_for_report" >
        </activity>
    </application>

</manifest>
